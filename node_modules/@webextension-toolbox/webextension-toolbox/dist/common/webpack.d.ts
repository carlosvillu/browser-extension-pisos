import Webpack from "webpack";
import { BuildCompileOptions, DevCompileOptions } from "./interfaces.js";
type WebpackConfigOptions = DevCompileOptions & BuildCompileOptions;
export default function webpackConfig({ src, target, packageTarget, dev, copyIgnore, compileIgnore, devtool, minimize, vendor, vendorVersion, manifestValidation, port, autoReload, swc, outputFilename, }: WebpackConfigOptions): Promise<Webpack.Configuration>;
export {};
